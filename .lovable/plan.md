

## Generate VeloDealer Favicon

1. **Generate a bike-themed favicon** using the Nano banana image generation API — a simple bicycle icon on a dark background, suitable for small 32x32 display.

2. **Save to `public/favicon.png`** and update `index.html` to reference it with the correct type.

### Files Changed
- `public/favicon.png` — New generated bike favicon
- `index.html` — Update `<link rel="icon">` to point to `/favicon.png`

